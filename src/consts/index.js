const STATUS = require('./status');
const EVENT_ACTIVITY_TYPE = require('./event-activity-type');
const EVENT_TYPE = require('./event-type');
const LOCATION_TYPE = require('./location-type');

module.exports = {
  STATUS,
  EVENT_ACTIVITY_TYPE,
  EVENT_TYPE,
  LOCATION_TYPE,
};
